{
  "name": "claude-slack-bridge",
  "version": "1.0.0",
  "description": "Bridge Slack messages to Claude Code stdin via tmux",
  "main": "bridge.js",
  "type": "module",
  "scripts": {
    "start": "node bridge.js"
  },
  "dependencies": {
    "@slack/bolt": "^3.17.0"
  }
}
